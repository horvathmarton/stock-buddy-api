from datetime import date
from src.stocks.models import Stock, StockPortfolio


def get_position_performance(stock: Stock, snapshot_dates: list[date]):
    pass


def get_portfolio_performance(
    portfolios: list[StockPortfolio], snapshot_dates: list[date]
):
    pass
